# ==============================================================
# EVENTOS68 - CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ==============================================================

# Entorno Django
DEBUG=True
SECRET_KEY=eventos68-secret-key-prod-and-dev-2026-safe-default-x92
ALLOWED_HOSTS=*

# Base de datos PostgreSQL (Neon.tech / Supabase)
# Si está vacío, Django usará automáticamente SQLite local para desarrollo
# Ejemplo Neon: postgresql://usuario:password@ep-xyz.us-east-2.aws.neon.tech/eventos68?sslmode=require
DATABASE_URL=

# Notificaciones por Correo (SMTP / Gmail / Resend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Eventos68 Catering <eventos68cr@gmail.com>

# Parámetros Comerciales por Defecto
DEFAULT_GANANCIA_PORCENTAJE=30.0
DEFAULT_PAGO_INICIAL_PORCENTAJE=10.0
DEFAULT_IVA_PORCENTAJE=13.0
